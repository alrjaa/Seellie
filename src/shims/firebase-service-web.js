/** Web stub for @/services/firebase — keep API shape, never initialize. */
export function getFirebaseApp() {
  return null;
}

export function getDb() {
  return null;
}

export const app = null;
export const db = null;
